server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/linktic_bd
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

api:
  key: micro123

productos:
  url: http://localhost:8081/productos/obtener
  timeout: 2000